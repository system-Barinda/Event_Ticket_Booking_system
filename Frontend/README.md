frontend/
│── index.html          (Homepage – list of events)
│── event.html          (Single event details + book button)
│── booking.html        (Seat selection & booking form)
│── mytickets.html      (User booked tickets)
│── login.html          (Login page)
│── register.html       (Register page)
│
├── css/
│     └── styles.css    (Your custom styles if needed)
│
├── js/
│     ├── api.js        (All backend API calls)
│     ├── auth.js       (Login/Register logic)
│     ├── events.js     (Load events on homepage)
│     ├── booking.js    (Seat booking logic)
│     └── tickets.js    (User tickets page)
│
└── assets/
      └── images/
